---
dependency:
  name: galaxy
  enabled: true
  options:
    force: true
driver:
  # see molecule_docker/playbooks for example
  name: podman
platforms:
  - name: "pdm4ans"
    image: "ghcr.io/raven428/container-images/systemd-debian13:latest"
    network_mode: bridge
    pre_build_image: true
    override_command: false
provisioner:
  name: ansible
  log: true
  env:
    ANSIBLE_VERBOSITY: 1
  inventory:
    links:
      group_vars: "group_vars"
  playbooks:
    prepare: prepare.yaml
    converge: converge.yaml
    verify: verify.yaml
  config_options:
    defaults:
      remote_tmp: /tmp
      jinja2_extensions: jinja2.ext.do
verifier:
  name: ansible
scenario:
  name: default
  create_sequence:
    - dependency
    - create
    - prepare
  converge_sequence:
    - converge
